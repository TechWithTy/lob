## Lob SDK environment example
# Test keys start with 'test_' and Live keys start with 'live_'
LOB_API_KEY=your_lob_api_key_here

# Optional: override base URL (defaults to https://api.lob.com/v1)
# LOB_BASE_URL=https://api.lob.com/v1

# Optional: pin API version (latest shown in docs is 2020-02-11)
# LOB_API_VERSION=2020-02-11

# Optional timeouts and retry config
# LOB_TIMEOUT=30
# LOB_MAX_RETRIES=3
# LOB_RETRY_DELAY=1.0